{
  "name": "dnd-interactive",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd src/server && npm install && npm start",
    "dev": "concurrently \"cd src/server && nodemon --openssl-legacy-provider\" \"cd src/client && npm run dev\"",
    "logs": "heroku logs -t",
    "heroku-postbuild": "cd src/client && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=12.16.1",
    "npm": ">=6.13.4"
  }
}
